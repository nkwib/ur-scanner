# Build artifacts (published from CI; rebuild locally with pnpm build / demo:build)
dist/
demo/dist/

# Test output
coverage/
test-results/
playwright-report/

# Installed via the pnpm workspace root
node_modules/
gif-out/

# Benchmark scratch (generated fake-camera video, bundles)
bench/.tmp/
